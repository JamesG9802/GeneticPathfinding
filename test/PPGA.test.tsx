describe("PPGA works when", () => {
    it("It can find the end", () => {
        // let graph: Graph2D = new Graph2D(5, 5);
        // let start_node: Node2D = { x: 0, y: 0};
        // let goal_node: Node2D = { x: 4, y: 4};

        // let connection_data: ValueMap<Node2D, Node2D> = PPGA<Node2D>(graph, start_node, goal_node, graph.manhattan_distance);
        // let path: Node2D[] = rebuild_path<Node2D>(connection_data, start_node, goal_node, Node2DEquals);
    
        // expect(path.length != 0).toBeTruthy();
    })
})